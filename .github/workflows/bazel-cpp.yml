name: Bazel C++ Port

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: Bazel C++ tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: bazel-contrib/setup-bazel@4fd964a13a440a8aeb0be47350db2fc640f19ca8 # v0.15.0
        with:
          bazelisk-cache: true
          disk-cache: true
          repository-cache: true
      - name: Test C++ port scaffold
        run: bazel test //cpp/tests:reco_control_tests
      - name: Test C++ port scaffold with ASan
        run: bazel test --config=asan //cpp/tests:reco_control_tests
      - name: Test C++ port scaffold with UBSan
        run: bazel test --config=ubsan //cpp/tests:reco_control_tests
