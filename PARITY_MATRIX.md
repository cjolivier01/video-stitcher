# C++ Port Parity Matrix

This matrix tracks Rust-to-C++ cutover gates. `Blocked` means the Rust module
remains authoritative and must not be removed.

| Area | Rust Reference | C++ Target | Current Status | Required Gates |
|---|---|---|---|---|
| Build | Cargo workspace | Bazel modules/packages | In progress | Bazel build/test in CI, sanitizer configs |
| reco-control | `crates/reco-control` | `cpp/reco_control` | In progress | Intent JSON goldens, pose tests, keyboard tests, GoPro URL/status tests |
| reco-core data/math | `crates/reco-core` non-GPU types | `cpp/reco_core` | In progress for `ViewportPosition`, rig correction, source path validation, frame data, calibration validation/JSON, point-in-polygon, virtual-camera yaw/pitch, scene geometry, scalar KB4 fisheye projection, camera/panorama projection chains, viewport bounds, telemetry aggregation, frame timing micros, event frame-index vocabulary, replay buffer policy, and calibration/playback performance parity reporting | Deferred: `CoverageBoundary`, GPU lens preview/undistort, WGSL/WebGPU renderer/session, replay management/recorder traits; add full lens/render GPU tests and executable calibration/playback performance parity gates |
| reco-core GPU | wgpu/WGSL render/session/interop | WebGPU/WGSL C++ | In progress for WGSL shader asset catalog, runfile validation, shader-facing render layout metadata, and CUDA driver-backed device/context/memory/2D-copy/kernel-launch/VMM shared-memory smoke integration | Deferred: WebGPU device/session, render pipeline, texture upload/import, CUDA/Vulkan external-memory interop, Metal/D3D11 interop, GPU golden pixel diffs, zero-copy tests; no CPU fallback in GPU gates |
| reco-io | FFmpeg/GStreamer/libcamera/V4L2/zero-copy | `cpp/reco_io` | In progress for output codec/quality/container policy, settings persistence/MRU, JSONL pipeline-event sink, CPU YUV420P stacked-video pack/unpack helpers for already CPU-resident frames, GStreamer/DeepStream/NvBufSurface runtime probes plus capture-pipeline contracts, Jetson NvBufSurface metadata extraction, GStreamer NVMM/NVDEC file-decode pipeline contracts, and GPU/NVMM decode frame-source contracts | Deferred: `StitchJob::run`, FFmpeg/GStreamer/DeepStream/libcamera/V4L2 ingest loops, encoder integration, CUDA import/export of NVMM surfaces, GPU atlas pack/unpack; add decode/encode smoke tests, camera ingest tests, zero-copy perf gates |
| reco-calibrate | AKAZE/audio/lens DB | `cpp/reco_calibrate` | In progress for config/data types, deterministic geometry/reprojection helpers, descriptor matching, spatial overlap filtering, frame sampling/downscale helpers, RANSAC fundamental-matrix inlier selection, default matcher/filter/cost policies, Nelder-Mead optimizer, lens-profile JSON lookup/search, pure telemetry math, CUDA/NVRTC device-resident Y-plane fisheye undistort for calibration frames, and GPU-resident calibration execution planning/probing | Deferred: GPU AKAZE detector/matcher execution, parser-backed GPU audio/video sampling, telemetry file extraction, embedded lens DB resource packaging; add fixtures, resource packaging, tolerance tests |
| reco-detect | ORT/TensorRT/NCNN/CoreML/CUDA/NPP | `cpp/reco_detect` | In progress for shared YOLO postprocess, ball-detector adapter/NMS, labels file parsing, ONNX names metadata parsing, AI probe result data helpers, CUDA P010/RGB/RGBA/NV12 preprocess kernels, NPP interop probing, ORT runtime probing, ORT CPU sessions, ORT CPU YOLO detector, ORT CUDA YOLO detector with GPU-resident preprocessing, TensorRT engine/context/binding runtime contract, TensorRT GPU YOLO detector integration, NCNN runtime/session contract, NCNN detector integration for preprocessed CHW tensors, and CoreML session/probe contract | Deferred: CoreML Objective-C++ prediction bridge; add broader backend probes and GPU preprocess tests |
| reco-autocam | panners/trackers/ROI | `cpp/reco_autocam` | In progress for `TrackingMode`, ROI anchor filtering policy, tracker coaster lifecycle, class provider, ball tracker, deterministic sweep panner, CSV file panner, and field panner policy | Deferred: WGPU detector wiring; add panner/trajectory fixture tests |
| reco-cli | `reco` binary | `cpp/apps/reco_cli` | In progress for command parsing across file stitch, preview, calibrate, live camera, libcamera, GoPro, and info surfaces; validation helpers; command dispatch; executable `info` capability reporting over CUDA, NPP, GStreamer/DeepStream/NvBufSurface, ORT, CoreML, and AI provider probes; `calibrate` now maps flags into a GPU-resident calibration request and reports required backend/stage readiness; remaining runtime commands now report command-specific GPU/zero-copy execution plans; file-based plans use the GStreamer NVMM/NVDEC decode contract | Deferred: stitch/preview/live-camera/libcamera/GoPro execution and full calibrate completion until GPU renderer, FFmpeg/GStreamer decode loops, GPU AKAZE calibration pipeline, camera control, and backend probes are ported; add help-text parity and golden CLI behavior matrix |
| reco-gui | Slint app | Qt app | In progress for persisted settings, toast state, telemetry payload, bug-report sizing data helpers, C++ app-shell state/export model with explicit GPU-preview readiness gating, Bazel-built Qt Widgets shell entrypoint, and shared CUDA/GStreamer/NPP runtime readiness reporting for preview/export | Deferred: GPU preview bridge, export worker, playback over FFmpeg/GStreamer, telemetry sender, UI workflow tests, asset/resource packaging |
| reco-obs | Rust cdylib + libobs bindings | `cpp/reco_obs` | In progress for module/source metadata, source defaults, property descriptors (replay descriptors opt-in), upstream source capability classification, source settings transition planning including unresolved-source and missing-core retries, input-format/replay-mode parsing, and OBS log-level mapping | Deferred: libobs ABI bindings, source plugin registration, async-frame ingestion, BGRA/YUV submit paths, replay recorder integration, frontend callbacks, OBS graphics upload/interop, UI properties runtime smoke tests; add ABI/header-version smoke tests before replacing Rust |
| Fuzz | `calibration_json`, `onnx_names`, `input_path` | C++ libFuzzer | In progress for `calibration_json`, `onnx_names`, and `input_path` harness builds | Run bounded libFuzzer jobs, sanitizer tested |
| Release | GitHub release workflow | Bazel release workflow | Blocked | Linux x86_64/arm64, macOS arm64/x86_64, Windows x86_64 packages |
