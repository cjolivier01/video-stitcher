# C++ Port Parity Matrix

This matrix tracks Rust-to-C++ cutover gates. `Blocked` means the Rust module
remains authoritative and must not be removed.

| Area | Rust Reference | C++ Target | Current Status | Required Gates |
|---|---|---|---|---|
| Build | Cargo workspace | Bazel modules/packages | In progress for C++ build/test and sanitizer configurations; current GPU/media feature `--define` values and dynamically loaded system libraries are not yet configured Bazel toolchains/dependencies | Add configured CUDA/NVCC, Dawn/WebGPU, NPP, GStreamer/DeepStream, FFmpeg, Qt, AI-runtime, and libobs dependencies with CI coverage |
| reco-control | `crates/reco-control` | `cpp/reco_control` | In progress | Intent JSON goldens, pose tests, keyboard tests, GoPro URL/status tests |
| reco-core data/math | `crates/reco-core` non-GPU types | `cpp/reco_core` | In progress for `ViewportPosition`, rig correction, source path validation, frame data, calibration validation/JSON, point-in-polygon, virtual-camera yaw/pitch, scene geometry, scalar KB4 fisheye projection, camera/panorama projection chains, viewport bounds, telemetry aggregation, frame timing micros, event frame-index vocabulary, replay buffer policy, and caller-supplied calibration/playback parity comparison math | Deferred: `CoverageBoundary`, GPU lens preview/undistort, WGSL/WebGPU renderer/session, replay management/recorder traits; add instrumentation, benchmarks, full lens/render GPU tests, and executable calibration/playback performance parity gates |
| reco-core GPU | wgpu/WGSL render/session/interop | Planned WebGPU/WGSL C++; current CUDA primitives | In progress for WGSL shader asset catalog, runfile validation, shader-facing render layout metadata, and CUDA driver-backed device/context/memory/2D-copy/kernel-launch/VMM shared-memory smoke integration; no C++ WebGPU device or render pipeline exists yet | Deferred: WebGPU device/session, render pipeline, texture upload/import, CUDA/Vulkan external-memory interop, Metal/D3D11 interop, GPU golden pixel diffs, zero-copy tests; no CPU fallback in GPU gates |
| reco-io | FFmpeg/GStreamer/libcamera/V4L2/zero-copy | `cpp/reco_io` | In progress for output codec/quality/container policy, settings persistence/MRU, JSONL pipeline-event sink, CPU YUV420P stacked-video pack/unpack helpers for already CPU-resident frames, GStreamer/DeepStream/NvBufSurface runtime probes plus pitch-linear NVMM capture/decode contracts, versioned DeepStream 7.1 and 9.1 NV12 surface validation, retained Jetson EGL/CUDA and CUDA-buffer mappings, direct dGPU CUDA-device views, preserved NV12 color metadata and stream rotation, negotiated visible dimensions distinct from padded NVMM allocation geometry, process-isolated parser-only H.264/HEVC video metadata probing with compressed-stream duration correlation and process-reclaimed timeout enforcement, GStreamer NVMM/NVDEC file-decode pipeline contracts, bounded accurate indexed seeking, GPU/NVMM decode frame-source contracts, and a dynamically loaded production appsink source that retains mapped samples through the NVMM-to-CUDA handoff | Deferred: `StitchJob::run`, FFmpeg/GStreamer/DeepStream/libcamera/V4L2 camera ingest loops, encoder integration, GPU atlas pack/unpack; add real encode smoke tests, camera ingest tests, and zero-copy perf gates |
| reco-calibrate | AKAZE/audio/lens DB | `cpp/reco_calibrate` | In progress for Linux/NVIDIA file calibration with explicit lens profiles and manual frame sync: config/data types, deterministic geometry/reprojection helpers, descriptor matching, spatial overlap filtering, frame sampling/downscale helpers, RANSAC fundamental-matrix inlier selection, default matcher/filter/cost policies, Nelder-Mead optimizer, lens-profile JSON lookup/search, pure telemetry math, CUDA/NVRTC device-resident full-resolution Y-plane fisheye undistort, indexed NVDEC/NVMM-to-CUDA extraction with calibration-owned device buffers, GPU AKAZE detection/description/matching with compact accepted-correspondence readback, stable input identity, isolated bounded worker execution, and atomic v1-compatible JSON publication | Deferred: non-Linux execution, GPU audio/IMU synchronization, telemetry file extraction, automatic profile selection, embedded lens DB resource packaging, live calibration, and debug image export; add default-workflow fixtures, resource packaging, tolerance tests, and calibration performance parity gates |
| reco-detect | ORT/TensorRT/NCNN/CoreML/CUDA/NPP | `cpp/reco_detect` | In progress for shared YOLO postprocess, ball-detector adapter/NMS, labels file parsing, ONNX names metadata parsing, AI probe result data helpers, CUDA P010/RGB/RGBA/NV12 preprocess kernels, matrix/range-aware BT.601/BT.709/BT.2020 CUDA and NPP NV12 conversion, NPP interop probing, ORT runtime probing, ORT CPU sessions, ORT CPU YOLO detector, ORT CUDA YOLO detector with GPU-resident preprocessing, TensorRT engine/context/binding runtime contract, TensorRT GPU YOLO detector integration, NCNN runtime/session contract, NCNN detector integration for preprocessed CHW tensors, and CoreML probe/session scaffolding; broad provider discovery is not executable yet | Deferred: CoreML Objective-C++ prediction bridge, DirectML, Metal/WGPU preprocessing, real provider discovery, and real-model backend tests; add broader backend probes and GPU preprocess tests |
| reco-autocam | panners/trackers/ROI | `cpp/reco_autocam` | In progress for `TrackingMode`, ROI anchor filtering policy, tracker coaster lifecycle, class provider, ball tracker, deterministic sweep panner, CSV file panner, and field panner policy | Deferred: WGPU detector wiring; add panner/trajectory fixture tests |
| reco-cli | `reco` binary | `cpp/apps/reco_cli` | In progress for parsing and plan dispatch across file stitch, preview, calibrate, live camera, libcamera, GoPro, and info surfaces; validation helpers; executable `info` capability reporting over CUDA, NPP, GStreamer/DeepStream/NvBufSurface, ORT, CoreML, and AI provider probes; executable Linux/NVIDIA file-backed `calibrate` with explicit lens profile and frame sync, GPU-resident decoding/undistortion/AKAZE, isolated workers, and atomic output; stitch/preview/camera/libcamera/GoPro deliberately return status 2 after reporting GPU/zero-copy plans | Deferred: automatic calibration profile/sync workflow and stitch/preview/live-camera/libcamera/GoPro execution until GPU renderer, camera control, remaining ingest/encode loops, and backend probes are ported; add help-text parity and golden CLI behavior matrix |
| reco-gui | Slint app | Qt app | In progress for persisted settings, toast state, telemetry payload, bug-report sizing data helpers, C++ app-shell state/export model with an explicit always-blocked GPU-preview gate, Bazel-built Qt Widgets shell entrypoint, and shared CUDA/GStreamer/NPP runtime readiness reporting for preview/export | Deferred: functional GPU preview bridge, export worker, playback over FFmpeg/GStreamer, telemetry sender, UI workflow tests, asset/resource packaging |
| reco-obs | Rust cdylib + libobs bindings | `cpp/reco_obs` | Metadata/planning only: module/source metadata, source defaults, property descriptors (replay descriptors opt-in), upstream source capability classification, source settings transition planning including unresolved-source and missing-core retries, input-format/replay-mode parsing, and OBS log-level mapping; no loadable C++ OBS plugin exists | Deferred: libobs ABI bindings, source plugin registration, async-frame ingestion, BGRA/YUV submit paths, replay recorder integration, frontend callbacks, OBS graphics upload/interop, UI properties runtime smoke tests; add ABI/header-version smoke tests before replacing Rust |
| Fuzz | `calibration_json`, `onnx_names`, `input_path`, probe CBOR IPC | C++ libFuzzer | In progress for `calibration_json`, `onnx_names`, `input_path`, and parser-probe request/response CBOR harnesses; probe CBOR runs bounded Clang/ASan libFuzzer sessions in CI with valid and malformed seed corpora | Extend bounded sanitizer fuzz execution to the remaining harnesses |
| Release | GitHub release workflow | Bazel release workflow | Blocked | Linux x86_64/arm64, macOS arm64/x86_64, Windows x86_64 packages |
